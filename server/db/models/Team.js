// TODO: will store team data here - namely the name, shortname, logo, and potentially playernames (later on)
// Will also have to make 'index.js' to store relations between teams/matches tables
const Sequelize = require('sequelize');
const db = require('../database');
